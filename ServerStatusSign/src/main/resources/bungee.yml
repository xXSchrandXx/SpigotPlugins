name: ${project.artifactId}
main: ${project.groupId}.bungee.${project.artifactId}
description: ${project.name}
version: ${project.version}
author: xXSchrandXx
website: https://www.gamestrike.de
commands:
  serverstatussignsbungee:
    description: Hauptkommando von ServerStatusSigns.
    aliases: [sssb]
    usage: /sssb <> []