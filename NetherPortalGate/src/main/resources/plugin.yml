name: ${project.artifactId}
main: ${project.groupId}.${project.artifactId}
description: ${project.name}
version: ${project.version}
author: xXSchrandXx
website: https://www.gamestrike.de
api-version: 1.13
commands:
  netherportalgate:
    description: Hauptkommando von NetherPortalGate.
    aliases: [npg]
    usage: /npg <> []